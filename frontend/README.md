
  # E-commerce app user interface

  This is a code bundle for E-commerce app user interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  